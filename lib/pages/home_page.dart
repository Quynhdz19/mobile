import 'package:flutter/material.dart';

class Hompage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar( title:Text("hompae"),
      ),
    );
  }
}
